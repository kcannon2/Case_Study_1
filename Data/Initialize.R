# Code Purpose: To initialize the R environment

# Libraries required to run the code
require(gdata)
require(plyr) 
library(downloader)
library(ggplot2)

# Download packages if necessary. Remove the # to use
# install.packages("downloader")
# install.packages("gdata")
# install.packages("ggplot2")
# install.packages("plyr")